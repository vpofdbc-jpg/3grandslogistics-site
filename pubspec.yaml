name: firestore_test_app
description: A new Flutter project for Firestore testing with stable, up-to-date Firebase dependencies.
publish_to: 'none' # Remove this line if you plan to publish your package to pub.dev

version: 1.0.0+1

environment:
  # Updated to the latest stable Dart SDK range
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8

  # 🚀 Updated to the latest FlutterFire stable versions (v2.x.x series)
  # This resolves known internal assertion bugs in older underlying Firebase JS SDKs.
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  firebase_auth: ^4.15.0
  firebase_messaging: ^14.7.4

  # 🔐 Firebase UI Auth (must be compatible with new core packages)
  firebase_ui_auth: ^1.12.0
  
  # 🛠️ Utilities
  intl: ^0.20.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # Using a modern lints version too

